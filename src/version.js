// src/version.js

/** The service name, as it appears in responses and logs. */
export const SERVICE = "genproj";

/** Service version. Kept in step with package.json. */
export const VERSION = "1.0.0";
